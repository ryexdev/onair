# NOTE: git does not support trailing comments on a pattern line. A comment
# must be on its own line, or it becomes part of the pattern and silently
# matches nothing. That mistake tracked three state files here once already.

# Runtime state: created on demand, specific to one setup and one location.
muted.json
bookmarks.json
spurs.json
cal.json

# Recordings. These are audio of real people on the air, and they are large.
*.wav
clips/
night/
stakeout/
recordings/

# Whisper models
models/
*.bin

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store
